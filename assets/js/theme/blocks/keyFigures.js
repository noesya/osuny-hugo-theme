window.blocks = window.blocks || {}

window.blocks.keyFigures = function () {
    var keyFigures = document.querySelectorAll('.block-key_figures');

    if (keyFigures.length <= 0) {
        return;
    }

    window.addEventListener('load', () => {
        
    });
    
};