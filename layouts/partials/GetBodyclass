{{- $bodyclass := .Params.bodyclass | default "default" -}}

{{- if ne $bodyclass "default" -}}
  {{- $bodyclass = printf "page-%s" $bodyclass -}}
{{- else -}}
  {{- $bodyclass = "" -}}
{{- end -}}

{{- return $bodyclass -}}